{
  "name": "lr1_parser",
  "version": "1.0.0",
  "description": "A CLI tool that reads grammars written in BNF format and generates standalone LR(1) parsers in TypeScript.",
  "main": "dist/main.js",
  "type": "module",
  "bin": {
    "lr1": "dist/main.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "mocha",
    "build": "tsc"
  },
  "author": "dospro",
  "license": "MIT",
  "devDependencies": {
    "@types/chai": "^5.2.3",
    "@types/mocha": "^10.0.10",
    "@types/node": "^26.3.0",
    "chai": "^6.2.2",
    "mocha": "^11.8.0",
    "typescript": "^7.0.2"
  }
}
